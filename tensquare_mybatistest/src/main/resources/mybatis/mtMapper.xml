<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//com.order.test.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tensquare.mybatistest.mapper.MtMapper">

    <resultMap id="book" type="com.tensquare.mybatistest.pojo.Book">
        <result column="s_id" property="id"></result>
        <result column="s_name" property="name"></result>
        <result column="s_birth" property="birth"></result>
        <result column="s_sex" property="sex"></result>
    </resultMap>

    <select id="getById" parameterType="string" resultMap="book">
        select * from student where s_id = #{id}
    </select>

    <!--<select id="selectBook2" resultType="com.wxdemo.po.Book">
        select * from book
        <where>
            <if test="id != null">
                id = #{id}
            </if>
            <if test="title != null">
                title = #{title}
            </if>
            <if test="price != null">
                price = #{price}
            </if>
            <if test="publicDate != null">
                publishDate = #{publicDate}
            </if>
        </where>
    </select>-->
</mapper>